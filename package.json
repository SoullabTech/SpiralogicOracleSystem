{
  "name": "spiralogic-oracle-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "start": "cd frontend && npm run start",
    "test": "cd frontend && npm test && cd ../backend && npm test",
    "lint": "cd frontend && npm run lint && cd ../backend && npm run lint",
    "lint:fix": "cd frontend && npm run lint:fix; cd ../backend && npm run lint:fix",
    "deploy": "echo 'Deploy step - implement based on your deployment strategy'",
    "prepare": "[ -d .git ] && husky install || true"
  },
  "devDependencies": {
    "husky": "^8.0.3"
  }
}
